# Don Goins Portfolio — Clean GitHub Pages Build

This build is intentionally simple:
- every file is in the repository root
- every HTML page uses the same `style.css`
- no game/demo files
- no nested folders

GitHub Pages:
- Source: Deploy from a branch
- Branch: main
- Folder: / (root)

Upload all files from this package directly into the repository root.
